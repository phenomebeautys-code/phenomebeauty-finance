// src/pages/Overview.tsx — Phase 1 cash wiring (excerpt around CashPositionCard)
// Replace the existing CashPositionCard call with this:

<CashPositionCard
  fnbCents={fnbCents}
  yocoSavingsCents={yocoSavingsCents}
  expectedPayoutCents={expectedPayoutCents}
  protectedCents={protectedCents}
  safeToUseCents={safeToUseCents}
/>
